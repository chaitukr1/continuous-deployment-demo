jobs:
  include:
    - stage: test
      script: echo head_branch
    - # stage name not required, will continue to use `test`
      script: echo test2
    - AfterSuccess:
    - stage: deploy
      script: echo deploy
